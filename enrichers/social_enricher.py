import requests

def enrich_social(entity: dict) -> dict:
    """
    Lightweight social discovery (placeholder for future expansion).
    Currently returns empty fields.
    """
    return {
        "facebook_url": None,
        "facebook_followers": None,
        "linkedin_url": None,
        "linkedin_employee_count": None,
    }
